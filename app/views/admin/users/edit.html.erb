<h1>Editing user</h1>

<% form_for [:admin, @user] do %>
  <%= render :partial => 'form' %>
  <%= submit_tag 'Edit' %>
<% end %>

<%= link_to 'Show', admin_user_url(@user) %> |
<%= link_to 'Back', admin_users_url %>
