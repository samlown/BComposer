<% for column in ContentPage.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @content_page.send(column.name) %>
</p>
<% end %>

<%= link_to _('Edit'), :action => 'edit', :id => @content_page %> |
<%= link_to _('Content Pages'), :action => 'list', :bulletin_id => @content_page.bulletin.id %>
